# Migrations

Canonical schema SQL now ships in a versioned migration file:

- `0001_canonical_schema_v1.sql`

## Rule

Keep `schemas/canonical/schema_v1.sql` and the latest migration aligned. If the canonical schema changes, update the migration path in the same change.
